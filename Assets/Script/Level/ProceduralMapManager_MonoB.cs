using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProceduralMapManager_MonoB : MonoBehaviour
{
    [SerializeField] MeshSettings_Scripta _meshSettings;
    [SerializeField] HeightMapSettings_Scripta _heightMapSettings;
    [SerializeField] string _message;
    MeshData mesh;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
    }
}
